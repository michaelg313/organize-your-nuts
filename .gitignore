# Secrets — the Storefront API token lives in .env and in Vercel's environment
# variables, never in the repo. (.env.example is the documented template and IS committed.)
.env
.env.*
!.env.example

# Installed packages and build output — regenerated by npm install / npm run build
node_modules/
dist/
.astro/
.vercel/

# Local editor / OS noise
.DS_Store
Thumbs.db
